# 项目开发规则

## 开源优先（强制）

实现或修改任何功能之前，必须先完成以下调查，不得直接从零手写：

1. 搜索是否已有可复用的开源项目、官方 SDK、框架能力或成熟依赖。
2. 优先检查项目官方仓库、官方文档和包管理器，不以聚合文章作为主要依据。
3. 至少评估以下内容：
   - 许可证是否允许当前用途；
   - 最近维护情况、发布版本和未解决问题；
   - Windows、浏览器、OBS 和当前 Next.js 版本的兼容性；
   - 安装体积、GPU/CPU、内存及付费 API 成本；
   - 安全风险、数据去向和密钥处理方式；
   - 接入工作量与后续维护成本。
4. 优先级依次为：
   - 现有项目已经提供的能力；
   - 官方功能或官方 SDK；
   - 活跃维护的成熟开源依赖；
   - 经过验证的小型开源实现；
   - 自行编写。
5. 只有在没有合适依赖、许可证不兼容、依赖明显过重或存在安全问题时，才允许自行实现。
6. 自行实现前必须在工作记录中说明为什么现有方案不可用。

## 依赖接入规则

- 引入依赖前检查当前稳定版本、许可证、维护活跃度和已知安全问题。
- 优先复用一个完整方案，避免同时引入多个功能重叠的库。
- 不复制开源项目的大段源码；优先通过包、插件、进程或清晰的适配层集成。
- 不把 API Key、令牌或候选人数据写入前端源码、日志或版本库。
- 新依赖必须写入对应的依赖清单和运行说明。
- 能通过 OBS Virtual Camera、浏览器标准 API 或会议软件标准设备接口解决的能力，不自行开发摄像头驱动。

## 每项功能的实施流程

1. 明确功能目标和运行环境。
2. 搜索并比较可用的开源方案和依赖。
3. 在 `docs/dependency-decisions.md` 记录选择结果。
4. 先做最小集成验证，再修改主流程。
5. 执行构建、类型检查和关键路径冒烟测试。
6. 在交付说明中列出复用的项目、许可证、配置方式和未解决限制。

紧急修复只有在不新增功能和依赖时可以跳过完整调研，但仍需完成构建或相关测试。
